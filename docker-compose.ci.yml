services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.37.0-focal
    working_dir: /usr/src/veterans/e2e
    volumes:
      - .:/usr/src/veterans/e2e
    command: npx playwright test
  # app:
  #   environment:
  #     - NODE_ENV=test
